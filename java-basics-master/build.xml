<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<!-- WARNING: Eclipse auto-generated file.
              Any modifications will be overwritten.
              To include a user specific buildfile here, simply create one in the same
              directory with the processing instruction <?eclipse.ant.import?>
              as the first entry and export the buildfile again. --><<project name="YourProject" default="compile" basedir=".">

    <!-- Define properties -->
    <property name="src.dir" value="src"/>
    <property name="build.dir" value="build"/>
    <property name="classes.dir" value="${build.dir}/classes"/>
    <property name="war.dir" value="war"/>
    <property name="war.file" value="YourProject.war"/>

    <!-- Define the compile target -->
    <target name="compile" description="Compile Java source files">
        <!-- Create the build directory if it doesn't exist -->
        <mkdir dir="${classes.dir}"/>

        <!-- Compile Java source files -->
        <javac srcdir="${src.dir}" destdir="${classes.dir}" includeantruntime="false"/>
    </target>

    <!-- Define the war target -->
    <target name="war" depends="compile" description="Create WAR file">
        <!-- Create the WAR directory if it doesn't exist -->
        <mkdir dir="${war.dir}"/>

        <!-- Create the WAR file -->
        <war destfile="${war.dir}/${war.file}" webxml="${src.dir}/WEB-INF/web.xml">
            <fileset dir="${src.dir}"/>
            <lib dir="${classes.dir}"/>
        </war>
    </target>

</project>
